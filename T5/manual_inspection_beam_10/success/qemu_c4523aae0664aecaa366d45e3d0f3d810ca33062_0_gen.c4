QVirtioPCIDevice *qvirtio_pci_device_find(QPCIBus *bus, uint16_t device_type)
{
    QVirtioPCIDevice *dev;
    qvirtio_pci_foreach(bus, device_type, false, 0, qvirtio_pci_assign_device, &dev);
    return dev;
}